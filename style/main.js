import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    header: {
      color: "white",
      fontSize: 25,
      paddingLeft: 25,
      flex: 5, 
      alignItems: "flex-start"
    }
  });